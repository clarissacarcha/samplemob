import React from 'react';
import {View, StyleSheet} from 'react-native';

export const SizedBox = () => <View style={styles.sizedBox} />;

const styles = StyleSheet.create({
  sizedBox: {
    height: 20,
  },
});
