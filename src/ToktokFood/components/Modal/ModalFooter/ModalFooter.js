/**
 * @format
 * @flow
 */

import React from 'react';

import type {PropsType} from './types';
import {} from './Styled';

const ModalFooter = (props: PropsType): React$Node => {
  return <></>;
};

export default ModalFooter;
